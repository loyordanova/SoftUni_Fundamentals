num = input().split(", ")

zero_count = []
count = 0
while count <= len(num):
    for n in num:
        if n == '0':
            num.remove(n)
            zero_count.append(n)
    count += 1
final = num + zero_count

final = [int(i) for i in final]


print(final)
